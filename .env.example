# API Configuration
# Note: All variables must be prefixed with VITE_ to be accessible in the client
# NEVER put sensitive data here - these are exposed to the browser!

# GraphQL API Base URL
VITE_API_BASE_URL=https://your-api-domain.com

# API Endpoints (relative paths)
VITE_GRAPHQL_ENDPOINT=/graphql
VITE_SCHEMA_SDL_ENDPOINT=/schema.graphql
VITE_SCHEMA_JSON_ENDPOINT=/schema.json
VITE_PLAYGROUND_ENDPOINT=/playground

# n8n Integration
VITE_N8N_EMAIL_WEBHOOK_URL=https://your-n8n-instance.com/webhook/email-processing

# File Upload Configuration
VITE_MAX_FILE_SIZE=10485760
VITE_ALLOWED_IMAGE_TYPES=image/png,image/jpeg,image/jpg,image/webp

# Optional: Environment identifier
VITE_APP_ENV=development

# Optional: API version (if your API supports versioning)
# VITE_API_VERSION=v1